[tool.poetry]
name = "kiwify"
version = "0.1.0"
description = ""
authors = ["Tulio <tulio.ax@hotmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
